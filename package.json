{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "mysql": "mysql -u root -p",
    "migrate": "mysql -u root -ppassword employees_db < db/schema.sql",
    "seed": "mysql -u root -ppassword employees_db < db/seeds.sql"
  },
  "keywords": [],
  "author": "Mila Decker",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.3.3",
    "mysql2": "^2.2.5"
  }
}
